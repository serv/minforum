<h1>Edit the reply</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for [@topic, @post] do |f| %>
      <!-- render 'shared/error_messages_topic' -->
      
      <%= render 'form', :f => f %>

      <%= f.submit "Edit", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>